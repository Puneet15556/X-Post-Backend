# Core backend
fastapi
uvicorn
python-dotenv
requests
pydantic
tweepy

# LangChain (Stable versions)
langchain==0.2.16
langchain-core==0.2.38
langchain-groq==0.1.0

# LangGraph (Removing fixed versions to prevent build failures)
langgraph
langgraph-checkpoint
langgraph-prebuilt

# MCP
mcp
langchain-mcp-adapters
